{
  "name": "shortplanner",
  "version": "1.0.0",
  "private": true,
  "description": "Stripboard och call sheets för filmproduktion",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "lint": "node scripts/check-syntax.mjs",
    "test": "node --test"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "better-sqlite3": "^11.5.0",
    "express": "^4.21.1"
  }
}
